<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/onboarding_full_root"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/demo_bg">

    <FrameLayout
        android:id="@+id/onboarding_full_ad_container"
        android:layout_width="match_parent"
        android:layout_height="match_parent" />

    <LinearLayout
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="top|end"
        android:gravity="center_vertical"
        android:orientation="horizontal"
        android:padding="8dp">

        <TextView
            android:id="@+id/onboarding_full_auto_skip"
            android:layout_width="wrap_content"
            android:layout_height="40dp"
            android:background="@drawable/bg_auto_skip_chip"
            android:gravity="center"
            android:minWidth="121dp"
            android:paddingHorizontal="12dp"
            android:text="@string/onboarding_full_auto_skip"
            android:textColor="@android:color/white"
            android:textSize="14sp"
            android:textStyle="bold"
            android:visibility="invisible" />

        <ImageButton
            android:id="@+id/onboarding_full_close"
            android:layout_width="48dp"
            android:layout_height="48dp"
            android:layout_marginStart="8dp"
            android:background="?attr/selectableItemBackgroundBorderless"
            android:contentDescription="@string/native_full_close"
            android:padding="4dp"
            android:scaleType="centerInside"
            android:src="@drawable/ic_close_circle"
            android:visibility="invisible" />
    </LinearLayout>

    <TextView
        android:id="@+id/onboarding_full_debug"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_gravity="bottom"
        android:background="@color/demo_scrim"
        android:padding="8dp"
        android:textAppearance="?attr/textAppearanceLabelSmall"
        android:textColor="@android:color/white"
        android:visibility="gone" />

</FrameLayout>
